import Ember from 'ember';

export default Ember.Controller.extend({
  sessions: ["slideout-goto",
             "slideout-marker",
             "slideout-line",
             "slideout-battery",
             "slideout-tie"] 
});
